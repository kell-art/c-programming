/*A programme to calculate area of a circle when radius is >=20*/ 
#include<stdio.h>
#include<math.h>
int main()
{
	float radius;
	float area;
	while(radius>=20);
	{
		printf("Enter the radius of a circle\n");
		scanf("%f",&radius);
		area = M_PI * radius * radius;
		printf("area is = %f",area);
		{
			if (area < 200);
			{
				printf("Less than normal");
				{
				if ("else");
					printf("build the nation");
				}
			}
		}
	}
	return 0;
}